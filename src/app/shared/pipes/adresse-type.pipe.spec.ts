import { AdresseTypePipe } from './adresse-type.pipe';

describe('AdresseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AdresseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
