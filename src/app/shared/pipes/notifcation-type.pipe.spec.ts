import { NotifcationTypePipe } from './notifcation-type.pipe';

describe('NotifcationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new NotifcationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
