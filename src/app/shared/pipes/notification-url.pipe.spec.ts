import { NotificationUrlPipe } from './notification-url.pipe';

describe('NotificationUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
