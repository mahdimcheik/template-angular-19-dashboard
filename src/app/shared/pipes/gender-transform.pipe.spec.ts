import { GenderTransformPipe } from './gender-transform.pipe';

describe('GenderTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
