import { RequiredAsteriskDirective } from './required-asterisk.directive';

describe('RequiredAsteriskDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredAsteriskDirective();
    expect(directive).toBeTruthy();
  });
});
