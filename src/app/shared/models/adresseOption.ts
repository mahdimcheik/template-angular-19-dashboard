/**
 * Type représentant une option d'adresse avec un identifiant, un nom et une valeur.
 * Utilisé pour les listes déroulantes d'adresses.
 */
export type AddressDropDown = {
    id: string;
    name: string;
    value: number;
};
